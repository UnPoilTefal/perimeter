@AGENTS.md

## Agent skills

### Issue tracker

Issues live as GitHub issues on UnPoilTefal/perimeter (`gh` CLI). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root, created lazily. See `docs/agents/domain.md`.
